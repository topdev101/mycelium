# Copy to `.env.local` to override the backend URL (defaults to localhost:8000).
# The Next.js dev server proxies /api/* to this address (see next.config.mjs).
NEXT_PUBLIC_API_BASE=http://localhost:8000
